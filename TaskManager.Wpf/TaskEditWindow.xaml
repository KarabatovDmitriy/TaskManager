<Window x:Class="TaskManager.Wpf.TaskEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование задачи" Height="400" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Название -->
        <TextBlock Grid.Row="0" Text="Название:" FontWeight="Bold" Margin="0,5"/>
        <TextBox x:Name="TitleBox" Grid.Row="1" Margin="0,0,0,8"/>

        <!-- Описание -->
        <TextBlock Grid.Row="2" Text="Описание:" FontWeight="Bold" Margin="0,5"/>
        <TextBox x:Name="DescriptionBox" Grid.Row="3" Height="80" 
                 TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,8"/>

        <!-- Приоритет и Статус в одной строке -->
        <Grid Grid.Row="4" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Приоритет:" FontWeight="Bold"/>
                <ComboBox x:Name="PriorityBox">
                    <ComboBoxItem Content="Низкий"/>
                    <ComboBoxItem Content="Средний"/>
                    <ComboBoxItem Content="Высокий"/>
                    <ComboBoxItem Content="Критический"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Статус:" FontWeight="Bold"/>
                <ComboBox x:Name="StatusBox">
                    <ComboBoxItem Content="Новая"/>
                    <ComboBoxItem Content="ВПроцессе"/>
                    <ComboBoxItem Content="Завершена"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Срок -->
        <TextBlock Grid.Row="5" Text="Срок выполнения:" FontWeight="Bold" Margin="0,5"/>
        <DatePicker x:Name="DeadlinePicker" Grid.Row="6" VerticalAlignment="Top" Margin="0,0,0,8"/>

        <!-- Важная задача -->
        <CheckBox x:Name="ImportantCheck" Grid.Row="6" Content="Важная задача" 
                  VerticalAlignment="Bottom" Margin="0,0,0,15"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,10,0" Click="OkButton_Click"/>
            <Button Content="Отмена" Width="75" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>